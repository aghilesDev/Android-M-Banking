<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.cnep.cnepe_banking">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".PresentationLayer.View.LoginView">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".PresentationLayer.View.Accueil"></activity>
        <activity android:name=".PresentationLayer.View.ListAgenceView"></activity>
        <activity android:name=".PresentationLayer.View.ListCompteView"></activity>
        <activity android:name=".PresentationLayer.View.ListCreditView" />
    </application>

</manifest>